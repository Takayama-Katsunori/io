machine:
  timezone:
    Asia/Tokyo
  environment:
    MAVEN_OPTS: -Xmx512m -XX:MaxPermSize=128m

dependencies:
  override:
#    - echo "skip mvn dependencies."
    - mvn install -B -pl es-api,common,logback -Dmaven.javadoc.skip=true -Dmaven.test.skip=true

test:
  override:
    - mvn test -B  -pl common -Ddependency.locations.enabled=false
#    # es-api
#    - mvn clean site -B -pl es-api -Ddependency.locations.enabled=false 
#
#    # common
#    - pushd common
#    - mvn clean site
#    - mvn install
#    - popd
#
#    # logback
#    - pushd logback
#    - mvn clean site
#    - mvn install
#    - popd
#
#    # core
#    - pushd core
#    - mvn clean site
#    - popd

